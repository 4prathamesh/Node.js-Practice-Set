    <!-- Navbar -->
    <nav class="bg-orange-500 text-white py-4 shadow-md">
        <div class="container mx-auto flex justify-between items-center px-4">
          <a href="/" class="text-2xl font-bold tracking-wide">
            Airbnb<span class="text-yellow-300">a</span>
          </a>
          <div class="space-x-6">
            <a href="/home" class="<%= currentPage === 'Home' ? 'bg-red-500' : 'hover:bg-red-500' %> py-2 px-4 rounded transition duration-300">Home List</a>

            <a href="/bookings" class="<%= currentPage === 'bookings' ? 'bg-red-500' : 'hover:bg-red-500' %> py-2 px-4 rounded transition duration-300">Bookings</a>

            <a href="/favourites" class="<%= currentPage === 'favourites' ? 'bg-red-500' : 'hover:bg-red-500' %> py-2 px-4 rounded transition duration-300">Favourites</a>

            <a href="/host/host-home-list" class="<%= currentPage === 'host-home' ? 'bg-red-500' : 'hover:bg-red-500' %> py-2 px-4 rounded transition duration-300">Host Homes</a>

            <a href="/host/add-home" class="<%= currentPage === 'addHome' ? 'bg-red-500' : 'hover:bg-red-500' %> py-2 px-4 rounded transition duration-300">Add Home</a>

          </div>
        </div>
      </nav>